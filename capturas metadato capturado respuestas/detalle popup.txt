basicamente modifique el codigo en la seccion de metadatos, de tal forma que al debugear ahora como se ve en la captura la respuesta del wms devuelve con el click event sobre una entidad (que debe estar incorporada como wmsSource), los metadatos alojados en la base. el fetch trae perfectamente la url especifica del servidor wms con los datos de la entidad seleccionada. Ahora el tema es, hay un paso previo a enviar estos datos al container "popup" que seria que en "res => data / res => json" al debugear en data deberia directamente aparecer devuelta en este caso en el formato seleccionado json (puede ser tmb html), el formato "amigable" con la interfaz usuario es json que provee tipo "lisado" la data. El tema es que la respuesta "res" no me muestra la "data" al debugear, a pesar de que si me trae la misma con la url. Esta parte hayq que verificar para saber si hay algun error en la respuesta de la data en formato json. Luego cuando esto da es mandar la data respuesta a la funcion "getFeatureInfoUrl" a los container popup que ya incorpore al codigo.

https://openlayers.org/en/latest/examples/popup.html (codigo fuente OpenLayers)
