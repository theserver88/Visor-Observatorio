en las capturas van a ver que la funcion del event click esta creada y funciona, de hecho en las caputras se ve que al dar click el evento trae por ejemplo las coordenadas y la resolucion, al hacer el debug la variable wmsSource indica que no está declarada, claro en la estructura del script, la parte donde se traen las capas, no estan decalradas como variable dado que estan dentro de la estructura de la funcion del "frame" creado para la sidebar (menu desplegable), entonces no llega a leer la variable wmsSource porque no esta declarada como var "wmsSource". El problema es que si yo declaro como var wmsSource cuando traigo determinada capa al mapa, da error porque no se puede traer con esa sentencia dentro de la funcion. En las capturas se ve bien. Lo unico faltaria es lograr como establecer la var wmsSource de, por ejemplo, aeropuertos (es la url wms del geoserver de donde trae la funcion "getFeatureInfoUrl" la data de la capa al hacer click evt. Una vez resuelto esto, y chequear que haceindo debug reconozca la url como los demas atributos geograficos, pasamos los datos a un container popup y ya aparecerian.